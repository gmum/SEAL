from setuptools import setup, find_packages

setup(
    name="SEAL",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    author="Sebastianyyy",
    description="SEAL project package",
)
